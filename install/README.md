# Install

No automatic install script is included yet.

Future helpers can sync selected `skills/core/*` and `skills/domain/*` folders to
`$HOME/.codex/skills`, but they should be explicit, idempotent, and avoid
overwriting local changes without a diff.

